<%= node["mconf-lb"]["deploy_to"] %>/shared/log/*.log {
         size 300M
         copytruncate
         rotate 30
         compress
         missingok
         notifempty
         su <%= node["mconf"]["user"] %> <%= node["mconf"]["app_group"] %>
}
